declare module 'electron-updater' {
  export const autoUpdater: any;
  // You might need to add more declarations here for other exports used from electron-updater
  // e.g., export interface UpdateInfo { ... }
}